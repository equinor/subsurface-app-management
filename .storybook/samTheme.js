import { create } from '@storybook/theming';

export default create({
  brandTitle: 'SAM storybook!',
  brandImage:
    'https://raw.githubusercontent.com/equinor/amplify-components/main/static/amplify_logo.png',
});
